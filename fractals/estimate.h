#ifndef _ESTIMATE_H_
#define _ESTIMATE_H_
 
#include <stdio.h>
#include <stdlib.h>

extern double avg2(int min_m, int max_m, int speed_m, int N, double min_eps, double max_eps, double speed_eps, double p, double * data, char namefileres[]);

#endif